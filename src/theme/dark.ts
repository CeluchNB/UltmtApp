import { Theme } from '../types/theme'
import { darkColors } from './colors'
import { size, weight } from './fonts'

const theme: Theme = { id: 'dark', colors: darkColors, size, weight }
export default theme
