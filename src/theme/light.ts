import { Theme } from '../types/theme'
import { lightColors } from './colors'
import { size, weight } from './fonts'

const theme: Theme = { id: 'light', colors: lightColors, size, weight }
export default theme
